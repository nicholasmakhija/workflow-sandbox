# Sample workflow for building and deploying a Next.js site to GitHub Pages
#
# To get started with Next.js see: https://nextjs.org/docs/getting-started
#
name: Build and Deploy

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["develop"]
    paths:
      - 'src/**'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Detect package manager
        id: detect-package-manager
        run: |
          if [ -f "${{ github.workspace }}/yarn.lock" ]; then
            echo "manager=yarn" >> $GITHUB_OUTPUT
            echo "command=install" >> $GITHUB_OUTPUT
            echo "runner=yarn" >> $GITHUB_OUTPUT
            exit 0
          elif [ -f "${{ github.workspace }}/package.json" ]; then
            echo "manager=npm" >> $GITHUB_OUTPUT
            echo "command=ci" >> $GITHUB_OUTPUT
            echo "runner=npx --no-install" >> $GITHUB_OUTPUT
            exit 0
          else
            echo "Unable to determine package manager"
            exit 1
          fi
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: ${{ steps.detect-package-manager.outputs.manager }}
      - name: Install dependencies
        run: ${{ steps.detect-package-manager.outputs.manager }} ${{ steps.detect-package-manager.outputs.command }}
      # - name: Build artifact
      #   run: ${{ steps.detect-package-manager.outputs.runner }} yarn build
      - name: Build artifact in temp folder
        run: |
          BUILD_DIR="${{ runner.temp }}/dist"
          mkdir -p $BUILD_DIR
          echo "Building source into temp directory..." > $BUILD_DIR/build_log.txt
          ${{ steps.detect-package-manager.outputs.runner }} yarn build --outDir ${{ runner.temp }}/dist
          cat $BUILD_DIR/build_log.txt
      - name: Show Folder Tree
        uses: jaywcjlove/github-action-folder-tree@main
        with:
          path: ${{ runner.temp }}/dist

  # Deployment job FIXME:
  # deploy:
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Deploy to GitHub Pages
  #       id: deployment
  #       uses: actions/deploy-pages@v4

  # Copy job
  # copy:
  #   needs: [build]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #     - id: date
  #       run: |
  #         echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT
  #     - uses: tuftsrt/copy-to-branch@v1
  #       with:
  #         destination-branch: main
  #         clear-destination: "true"
  #         copy: |
  #           dist/|
