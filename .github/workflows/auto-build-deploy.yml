name: Trigger build and deploy

on:
  push:
    branches:
      - develop
    paths:
      - 'src/**'

jobs:
  call-build-deploy:
    uses: ${{ github.repository_owner }}/.github/workflows/build-deploy.yml@main